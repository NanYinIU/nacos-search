# Verification gaps after closed issues #2–#14

Independent Spec review found several engines present but product paths incomplete.
CI V3 smoke fixed in PR #16 (`NACOS_AUTH_ADMIN_ENABLE=false`).

Work these tickets blockers-first. Prefer `/implement` + `/tdd` per ticket.
