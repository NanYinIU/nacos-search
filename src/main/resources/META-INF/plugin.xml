<idea-plugin>
    <id>com.nanyin.nacos.search</id>
    <name>Nacos Search</name>
    <vendor email="nanyiniu@gmail.com" url="https://github.com/NanYinIU">Nacos Viewer by nanyin</vendor>

    <!-- description and change-notes are owned by patchPluginXml in build.gradle.kts (#155) -->

   <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Tool Window -->
        <toolWindow id="Nacos Search" 
                   secondary="true" 
                   icon="/icons/nacosSearch_20.svg"
                   anchor="right" 
                   factoryClass="com.nanyin.nacos.search.ui.NacosSearchToolWindowFactory"/>
        
        <!-- Settings -->
        <applicationConfigurable 
            parentId="tools" 
            instance="com.nanyin.nacos.search.settings.NacosConfigurable"
            id="com.nanyin.nacos.search.settings.NacosConfigurable"
            displayName="Nacos Search"/>
        
        <!-- Project startup activity -->
        <postStartupActivity implementation="com.nanyin.nacos.search.NacosSearchPlugin"/>

        <!-- ADR-0027: veto project close while a dirty configuration draft is held -->
        <projectCloseHandler implementation="com.nanyin.nacos.search.ui.DirtyDraftProjectCloseHandler"/>

        <!-- @NacosValue navigation: reference contribution, line marker, reverse Find Usages -->
        <psi.referenceContributor
            language="JAVA"
            implementation="com.nanyin.nacos.search.psi.NacosValueReferenceContributor"/>

        <codeInsight.lineMarkerProvider
            language="JAVA"
            implementationClass="com.nanyin.nacos.search.psi.NacosValueLineMarkerProvider"/>

        <referencesSearch
            implementation="com.nanyin.nacos.search.psi.NacosConfigKeyReferenceSearcher"/>

        <!-- Persistent index: placeholder key -> .java files for fast reverse lookup -->
        <fileBasedIndex
            implementation="com.nanyin.nacos.search.psi.NacosPlaceholderIndex"/>

        <!-- Persistent index: declared configuration-source dataId -> .java files -->
        <fileBasedIndex
            implementation="com.nanyin.nacos.search.psi.NacosDeclaredSourceIndex"/>

        <!-- Services are now registered via @Service annotations -->
    </extensions>

    <actions>
        <!-- Refresh Cache Action -->
        <action id="com.nanyin.nacos.search.actions.RefreshCacheAction"
               class="com.nanyin.nacos.search.actions.RefreshCacheAction"
               text="Refresh Cache" 
               description="Refresh Nacos configuration cache">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        
        <!-- Clear Cache Action -->
        <action id="com.nanyin.nacos.search.actions.ClearCacheAction"
               class="com.nanyin.nacos.search.actions.ClearCacheAction"
               text="Clear Cache" 
               description="Clear Nacos configuration cache">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
