# 按访问身份隔离缓存

Nacos Search 的缓存除服务器与配置坐标外，还必须按环境配置、认证模式和用户名组成的访问身份隔离；匿名访问也是独立身份。身份切换或认证失败后不得展示其他身份留下的缓存，因为提高缓存命中率不能以跨权限边界暴露历史配置为代价，旧身份数据只能保持不可见并等待清理。升级时无法证明访问身份归属的旧格式配置缓存一次性废弃，不猜测迁移；清理必须可中断、可重复，失败不能阻塞插件启动。
